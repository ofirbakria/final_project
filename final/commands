helm upgrade finalproject -f .\values.yaml -n oferbakria .
helm install finalproject -f .\values.yaml -n oferbakria .
helm uninstall finalproject -n oferbakria .

kubectl create secret generic aws-credentials --from-file=credentials=C:\Users\User\.aws\credentials -n oferbakria

https://api.telegram.org/bot<your-bot-token>/setWebhook?url=https://<your-load-balancer-url>/your-webhook-path
docker run -it --rm poly bash


kubectl version --client
helm version

#################################################################################
create new ssh key for github
ssh-keygen -t ed25519 -C "ofir.bakria@gmail.com"
add publict the githubssh
add private in jenkins pipline
add trusted push to github from node using command :
ssh-keyscan github.com >> ~/.ssh/known_hosts

################################
for webhook
GitHub Integration Plugin on jenkins Plugin
"" "" Generic Webhook Trigger Plugin then reboot




